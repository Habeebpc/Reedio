version: "3.8"

services:
  django:
    build: .
    container_name: django
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/srv/boilerplate/app/
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: "redis:alpine"
    container_name: redis

  celery:
    build: .
    command: celery -A boilerplate worker -l info -Q boilerplate
    volumes:
      - .:/srv/boilerplate/app/
    depends_on:
      - django
      - redis

  celery-beat:
    build: .
    command: celery -A boilerplate beat -l info
    volumes:
      - .:/srv/boilerplate/app/
    depends_on:
      - django
      - redis
